<server description="Collective Member - membro1-replica2">
    <featureManager>
        <feature>clusterMember-1.0</feature>
    </featureManager>

    <httpEndpoint httpPort="9080" httpsPort="9443" id="defaultHttpEndpoint" host="*" />

    <!-- Collective member configuration -->
    <collectiveMember
        replicaSetName="Replica2"
        hostName="membro1-replica2"
        serverUser="admin"
        serverPassword="adminPassword"
        controllerUser="admin"
        controllerPassword="adminPassword"
        controllerHost="controlador"
        controllerPort="9443" />

    <!-- Default SSL configuration -->
    <ssl id="defaultSSLConfig" keyStoreRef="defaultKeyStore" />
    <keyStore id="defaultKeyStore" password="keyStorePassword" />
</server>
